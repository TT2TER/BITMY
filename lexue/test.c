#include<stdio.h>
#include<string.h>
int main()
{
    int i;
    int j;
    int len;
    char num[11000];
    char name[11][22];
    for (i = 1; i <= 2; i++)
    {
        scanf("%s", name[i]);
        scanf("%s",&num[0]);

    }
    puts(&num[0]);
    return 0;
}