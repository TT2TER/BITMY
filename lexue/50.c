#include<stdio.h>
#define wl wordlist
int start[10000];
int end[10000];
int main()
{
    while(getchar()!=EOF)
    // int *p=new int (10);//new 是动态变量的标志 动态变量没有变量名 
    // delete p;//杀死动态变量
    return 0;
}